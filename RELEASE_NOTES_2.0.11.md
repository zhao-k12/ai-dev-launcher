# AI Dev Launcher v2.0.11

## Fixes

- Hide the expected local Headroom WebSocket 403 retry sequence.
- Hide the matching WebSocket-to-HTTPS fallback event.
- Preserve genuine authentication, HTTP, proxy, and network failures.
- Keep the successful assistant response and task completion visible.

