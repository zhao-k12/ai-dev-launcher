# AI Dev Launcher v2.0.10

## Fixes

- Carry the resolved Node.js directory into private Codex version checks as
  well as conversation processes.
- Support uppercase Windows environment keys such as `PROGRAMFILES`.
- Verified with an intentionally stripped desktop PATH: the frozen runtime
  completed a real Headroom-routed Codex request with exit code 0.

